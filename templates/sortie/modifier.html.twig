{% extends 'base.html.twig' %}

{% block title %}
    <h1>Créer une sortie</h1>
{% endblock %}

{% block body %}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alter-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
    <h1 class="centrer">Modifier une sortie</h1>

    <div class="centrer">
        {{ form_start(modifierSortieForm) }}

        <div class="flex-row">
            <label>{{ field_label(modifierSortieForm.nom) }} :</label>
            <div>{{ form_widget(modifierSortieForm.nom) }}</div><br>
        </div>

        <div class="flex-row">
            <label>{{ field_label(modifierSortieForm.dateHeureDebut) }} :</label>
            <div>{{ form_widget(modifierSortieForm.dateHeureDebut) }}</div><br>
        </div>

        <div class="flex-row">
            <label>{{ field_label(modifierSortieForm.dateLimitInscription) }} :</label>
            <div>{{ form_widget(modifierSortieForm.dateLimitInscription) }}</div><br>
        </div>

        <div class="flex-row">
            <label>{{ field_label(modifierSortieForm.nbInscriptionsMax) }} :</label>
            <div>{{ form_widget(modifierSortieForm.nbInscriptionsMax) }}</div><br>
        </div>

        <div class="flex-row">
            <label>{{ field_label(modifierSortieForm.duree) }} :</label>
            <div>{{ form_widget(modifierSortieForm.duree) }}</div><br>
        </div>

        <div class="flex-row">
            <label>{{ field_label(modifierSortieForm.InfosSortie) }} :</label>
            <div>{{ form_widget(modifierSortieForm.InfosSortie) }}</div><br>
        </div>

        <div class="flex-row">
            <label>{{ field_label(modifierSortieForm.site) }} :</label>
            <div>{{ form_widget(modifierSortieForm.site) }}</div><br>
        </div>

        <div class="flex-row">
            <label>{{ field_label(modifierSortieForm.lieu) }} :</label>
            <div>{{ form_widget(modifierSortieForm.lieu) }}</div><br><br>
        </div>

        <div class="flex-row" >
            <div>{{ form_widget(modifierSortieForm.Enregistrer) }}</div>
            <div>{{ form_widget(modifierSortieForm.Publier) }}</div>
            <a class="btn" href="{{ path('sortie_list') }}">Annuler</a>

        </div>
        {{ form_end(modifierSortieForm) }}

    </div>
{% endblock %}